import { Link,useSearchParams } from 'react-router-dom';import { CheckCircle,MessageCircle } from 'lucide-react';import { whatsappUrl } from '../lib/api.js';export default function OrderSuccess(){const [params]=useSearchParams();const order=params.get('order');return <main className="section-shell py-24 text-center"><CheckCircle className="mx-auto text-[#a47638]" size={58}/><h1 className="mt-6 font-display text-5xl">Order received.</h1><p className="mx-auto mt-4 max-w-xl leading-7 text-stone-500">Your order {order&&<strong>#{order}</strong>} has been recorded. Continue the WhatsApp conversation so we can confirm delivery and dispatch.</p><a href={whatsappUrl()} target="_blank" rel="noreferrer" className="mt-7 inline-flex items-center gap-2 rounded-full bg-[#168c43] px-6 py-3.5 font-bold text-white"><MessageCircle size={18}/> Open WhatsApp</a><div><Link to="/shop" className="mt-5 inline-block font-bold text-[#a47638]">Continue shopping</Link></div></main>}
