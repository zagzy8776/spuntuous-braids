import { useEffect, useState } from 'react';
import { Search } from 'lucide-react';
import { api } from '../lib/api.js';
import ProductCard from '../components/ProductCard.jsx';
export default function Shop(){const [products,setProducts]=useState([]);const [q,setQ]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{api.get('/products?limit=100').then(r=>setProducts(r.data.products||[])).catch(()=>setProducts([])).finally(()=>setLoading(false));},[]);const filtered=products.filter(p=>`${p.name} ${p.category?.name||''}`.toLowerCase().includes(q.toLowerCase()));return <main className="section-shell py-14"><div className="flex flex-wrap items-end justify-between gap-6"><div><p className="text-xs font-bold uppercase tracking-[.3em] text-[#a47638]">Shop</p><h1 className="font-display text-5xl">Branded hair products</h1><p className="mt-3 max-w-2xl text-stone-500">Shop Sumptuous Braids products directly. Orders are recorded and sent into the business workflow for confirmation and dispatch.</p></div><div className="flex items-center gap-2 rounded-full bg-white px-4 py-3 ring-1 ring-black/5"><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products" className="w-40 bg-transparent outline-none"/></div></div><div className="mt-10 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>{loading&&<p className="py-12 text-center text-stone-500">Loading collection…</p>}{!loading&&!filtered.length&&<p className="py-12 text-center text-stone-500">No products match your search.</p>}</main>}
