import { useEffect,useState } from 'react';import { Link,useNavigate } from 'react-router-dom';import { api } from '../../lib/api.js';const auth=()=>({headers:{Authorization:`Bearer ${localStorage.getItem('sb_admin_token')}`}});export default function AdminDashboard(){const nav=useNavigate();const [stats,setStats]=useState(null);useEffect(()=>{api.get('/orders/stats/summary',auth()).then(r=>setStats(r.data)).catch(()=>{localStorage.removeItem('sb_admin_token');nav('/admin/login')})},[]);return <main className="min-h-screen bg-[#f4efe7]"><header className="border-b bg-white"><div className="section-shell flex items-center justify-between py-4"><div><p className="text-xs font-bold uppercase tracking-[.25em] text-[#a47638]">Command center</p><h1 className="font-display text-2xl">Sumptuous Braids</h1></div><div className="flex gap-2"><Link to="/" className="rounded-full border px-4 py-2 text-sm font-bold">View site</Link><button onClick={()=>{localStorage.removeItem('sb_admin_token');nav('/admin/login')}} className="rounded-full bg-[#15130f] px-4 py-2 text-sm font-bold text-white">Sign out</button></div></div></header><div className="section-shell py-10"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Revenue',`₦${Number(stats?.revenue||0).toLocaleString()}`],['Orders',stats?.orders||0],['Products',stats?.products||0],['Pending',stats?.pending||0]].map(([a,b])=><div className="rounded-3xl bg-white p-6 card-shadow" key={a}><p className="text-sm text-stone-500">{a}</p><strong className="mt-2 block text-3xl">{b}</strong></div>)}</div><div className="mt-8 grid gap-4 md:grid-cols-3"><Link to="/admin/products" className="rounded-3xl bg-[#15130f] p-7 text-white"><h2 className="font-display text-2xl">Products & stock</h2><p className="mt-2 text-sm text-stone-400">Add products, pricing, images and inventory.</p></Link><Link to="/admin/orders" className="rounded-3xl bg-white p-7 card-shadow"><h2 className="font-display text-2xl">Orders & dispatch</h2><p className="mt-2 text-sm text-stone-500">Confirm orders and update dispatch status.</p></Link><div className="rounded-3xl bg-[#e7dbc7] p-7"><h2 className="font-display text-2xl">Reports</h2><p className="mt-2 text-sm text-stone-600">Revenue and operational counters are surfaced here for daily decisions.</p></div></div></div></main>}
