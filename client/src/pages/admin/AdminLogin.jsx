import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { api } from '../../lib/api.js';export default function AdminLogin(){const nav=useNavigate();const [form,setForm]=useState({email:'',password:''});const [error,setError]=useState('');async function submit(e){e.preventDefault();setError('');try{const r=await api.post('/auth/login',form);localStorage.setItem('sb_admin_token',r.data.token);nav('/admin');}catch(err){setError(err?.response?.data?.message||'Unable to sign in.')}}return <main className="min-h-screen bg-[#15130f] px-4 py-16"><form onSubmit={submit} className="mx-auto max-w-md rounded-[2rem] bg-[#f8f4ed] p-8"><p className="text-xs font-bold uppercase tracking-[.3em] text-[#a47638]">Sumptuous Braids</p><h1 className="mt-3 font-display text-4xl">Admin sign in</h1>{error&&<p className="mt-4 rounded-2xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<label className="mt-6 block text-sm font-semibold">Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="mt-2 w-full rounded-2xl border border-black/10 bg-white px-4 py-3"/></label><label className="mt-4 block text-sm font-semibold">Password<input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="mt-2 w-full rounded-2xl border border-black/10 bg-white px-4 py-3"/></label><button className="mt-6 w-full rounded-full bg-[#15130f] py-3.5 font-bold text-white">Sign in</button></form></main>}
