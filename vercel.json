{
  "version": 2,
  "buildCommand": "cd server && npm install --include=dev && npx prisma generate && npx prisma db push && node prisma/seed.js && cd ../client && npm install --include=dev && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
